service: hello-go-gin-lambda

provider:
  name: aws
  region: us-east-1
  runtime: go1.x
  architecture: arm64

package:
  exclude:
    - ./**
  include:
    - ./bin/**

functions:
  main:
    handler: bin/main
    events:
      - http:
          path: hello
          method: post
